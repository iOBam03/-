# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Environment
.env
.env.local

# Local prototype config (contains API keys from .env)
config.local.js

# Real procurement files (contain sensitive project data — never commit)
*.XLSX
*.xlsx
*.XLS
*.xls
!js/supplier-comparison.js
!js/data.js
# Allow mock fixtures สำหรับ test multi-BOQ comparison (generate by tools/generate-mock-multi-boq.js)
!tests/fixtures/*.xlsx

# Node / build artifacts (ไม่ใช้ในโปรเจคนี้ แต่กันไว้)
node_modules/
dist/
build/

# npm files (web app ใช้ CDN, build script parse XML ตรงๆ ไม่ต้อง install)
package.json
package-lock.json

# Vercel
.vercel/
.vercel
