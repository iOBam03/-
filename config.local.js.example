// config.local.js.example
// Copy this file to `config.local.js` (gitignored) and fill in your values.
// ไฟล์นี้จะถูก include ก่อน js/data.js ใน index.html และ alerts.html
// หากไม่มีไฟล์ config.local.js ระบบจะยังทำงานได้ (จะเตือนใน console)

window.LOCAL_CONFIG = {
  // ตัวอย่าง: API key ส่วนตัว
  // SAP_API_KEY: '...',
  // SUPABASE_URL: '...',

  // ---------- AI Scan (สแกนเอกสารกระดาษเป็น BOQ ใน alerts.html) ----------
  // ขอ key ฟรีได้ที่ https://aistudio.google.com/apikey
  // ถ้าไม่ตั้ง — ปุ่ม "สแกนเอกสาร (AI)" จะแสดง toast error แทน
  // GEMINI_API_KEY: 'AIza...',

  // ปิด warning banner (ถ้าต้องการ)
  // SILENT_LOCAL_CONFIG_MISSING: true
};